# Which provider answers chat: openrouter (default) or gemini
CHAT_PROVIDER=openrouter
# OpenRouter (https://openrouter.ai) — server-side only, never NEXT_PUBLIC
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-oss-120b
# Gemini (fallback provider)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash